一、系统定位（给 AI 的总说明）

这是一个 交易风控 / 行为监控 Web 管理后台，
用于 从 MT4 / MT5 抓取交易数据，
基于 可配置规则，
对 异常交易行为 进行 识别、告警、统计和管理。

系统特点：

多交易平台（MT4 / MT5）

多账户币种（USD / JPY / EUR / GBP / NZD / CAD 等）

多异常类型（Large Trade / Liquidity Trade / Scalping / Rebate Churning）

强配置化（非写死规则）

面向 运营 / 风控人员 使用

二、核心模块划分（后台菜单结构）
Web Admin
├── Dashboard（总览）
├── 规则管理（Rule Engine）
│   ├── Large Trade
│   ├── Liquidity Trade
│   ├── Scalping
│   └── Rebate Churning
├── 产品 & 品类映射（Product Mapping）
├── 监控记录（Alert / Log）
├── 账户管理（Trading Accounts）
├── 参数配置（Global Settings）
└── 权限管理（可选）

三、数据基础模型（AI 编程重点）
1️⃣ 交易基础数据（从 MT4 / MT5）
Trade {
  trade_id
  platform: MT4 | MT5
  account_id
  account_currency
  product_code
  product_category
  volume_lot
  open_time
  close_time
  deal_usd
}


说明：

deal_usd = 已统一换算为 USD（后台需有汇率换算）

MT4 使用 trades，MT5 使用 deals

一个交易账户 ≠ 一个用户（是风控维度）

2️⃣ 产品映射表（Product Mapping）

用于解决：

不同平台产品名字不同，但业务要按统一大类判断

ProductMapping {
  platform
  raw_product_name
  unified_product_code
  product_category
  enabled: true | false
}


后台能力：

开 / 关某些产品参与风控计算

不同平台可映射到同一产品大类

四、规则系统（最核心）
统一规则模型
Rule {
  rule_id
  rule_type
  enabled
  parameters
  filters
  trigger_action
}

五、各规则的后台逻辑定义
🧠 1. Large Trade（大额交易）
规则目标

识别：单个账户，在某个产品上，短时间内出现异常大的交易金额

后台可配置参数
parameters: {
  time_window_seconds
  min_deal_usd
}

可选过滤器（filters）
filters: {
  product_list
  account_currency_list
}

触发逻辑（给 AI）
在 time_window_seconds 内，
同一 account_id + product，
若 ∑ deal_usd ≥ min_deal_usd
→ 触发告警

🧠 2. Liquidity Trade（拆单流动性交易）
规则目标

识别：短时间内多笔小单，但总量异常大

后台可配置参数
parameters: {
  time_window_seconds
  min_order_count
  min_total_volume_lot
}

触发逻辑
在 time_window_seconds 内，
同一 account_id + product：
- 订单数量 ≥ min_order_count
- ∑ volume_lot ≥ min_total_volume_lot
→ 触发告警

🧠 3. Scalping（超短线）
规则目标

识别：持仓时间过短的交易行为

后台可配置参数
parameters: {
  max_holding_seconds
}

可选过滤器
filters: {
  product_list
  min_volume_lot
  min_deal_usd
}

触发逻辑
holding_time = close_time - open_time

若 holding_time ≤ max_holding_seconds
且满足 filters
→ 触发告警

🧠 4. Rebate Churning（返佣刷单）
规则目标

识别：IB 通过大量交易刷 rebate

前提

Broker 可提供 rebate 数据

后台参数
parameters: {
  date_range
}

统计逻辑
按 IB 维度：
- 统计历史 + 当日 rebate
- 按 rebate 金额降序排列

后台展示重点

排名

异常 IB 高亮

支持导出

六、告警 & 记录模块（Alert Center）
告警数据结构
Alert {
  alert_id
  rule_type
  account_id
  product
  trigger_time
  trigger_value
  status: new | reviewed | ignored
}


后台功能：

列表查看

按规则 / 产品 / 时间筛选

人工标记（已查看 / 忽略）

七、全局配置（Global Settings）
GlobalSettings {
  base_currency = USD
  exchange_rate_source
  default_time_zone
}

---
## 新增需求（2025-12-13）

### 1. 数据源管理
对于 MT5和 MT4，这个 web 管理平台，会对接多个 MT5 平台，多个 MT4 平台。他们分别对应不同的公司，产品的命名会不一样，规则的配置也不一样。

### 2. 权限账号管理
对于不同的 MT5 公司，我会给他们开具不同的账号权限，让他们去配置不同规则，我作为总的管理员，可以看到全部。

### 3. 需求背景
这两个新增需求：主要针对 web 面向多个 MT5数据源，MT4 数据源的情景。

### 4. 需求确认（讨论结果）
- 数据隔离：完全隔离（公司间不可见）
- 规则配置范围：数据源级别（每个数据源独立配置）
- 产品映射：每个数据源独立配置
- 角色设计：需要只读角色，公司内多角色（管理员/普通用户/只读）
- 登录方式：模拟切换用户视角
