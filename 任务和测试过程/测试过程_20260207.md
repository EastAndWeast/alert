# 测试过程记录 - 推送样式字段同步

## 变更说明
已将“推送样式”管理模块的默认模板更新为与“告警记录”字段一致。

### 字段对应关系
| 中文标签 | 英文标签 | 占位符 |
| :--- | :--- | :--- |
| 告警ID | Alert ID | `{alert_id}` |
| 触发时间 | Trigger Time | `{trigger_time}` |
| 账户ID | Account ID | `{account_id}` |
| 产品 | Product | `{product}` |
| 触发值 | Triggered Value | `{trigger_value}` |
| 详情 | Details | `{details}` |

## 验证结果
1. 推送样式中的字段标签现已与告警详情页保持一致。
2. 默认模板已更新。
