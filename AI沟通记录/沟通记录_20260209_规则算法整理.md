# AI 沟通记录 - 规则算法整理

**日期**：2026-02-09
**参与者**：User, Antigravity (AI)

## 沟通背景
用户要求对项目中 10 个风控告警规则的底层逻辑进行整理，重点关注“触发值”的计算方式以及“概要（配置摘要）”的提取逻辑，并输出正式文档。

## 讨论要点
1.  **触发值算法**：
    *   AI 分析了 `rules.js` 中的 `formatTriggerValue` 函数。
    *   明确了不同规则（如 Large Trade, Scalping, Exposure 等）如何根据订单属性（手数、盈亏、时间）计算并格式化显示数值。
2.  **概要算法**：
    *   AI 分析了 `rules.js` 中的 `renderRuleParams` 函数。
    *   整理了各规则卡片上显示的配置项摘要（如阈值、监控范围、时间窗口等）。

## 结论与行动
*   **行动**：AI 生成了《告警规则算法说明文档.md》并存放在“文档”文件夹下。
*   **结果**：文档覆盖了全部 10 个规则，清晰界定了算法边界。

---
*记录保存于 AI沟通记录 文件夹*
