# 任务清单 - 多语言切换功能

## 阶段 1: 调研与分析 (当前阶段)
- [x] 调研项目现有多语言方案 (`js/i18n.js`)
- [x] 扫描项目中硬编码的中文文本 (HTML & JS)
- [x] 确认 `npx uipro-cli init` 初始化状态
- [/] 编写实施计划 (`implementation_plan.md`)

## 阶段 2: 基础配置
- [ ] 修改 `js/i18n.js` 默认语言为 `zh` (中文)
- [ ] 扩展 `js/i18n.js` 中的词条库，包含所有提取的中文及其对应英文翻译
- [ ] 在 `js/i18n.js` 中增加通用的 DOM 翻译工具函数

## 阶段 3: HTML 及 JS 迁移
- [ ] 改造 `index.html`，使用 `data-i18n` 属性标记需要翻译的文本
- [ ] 改造 `login.html`，支持双语显示
- [ ] 重构各个 JS 模块 (如 `rules.js`, `alerts.js`)，将硬编码中文替换为 `I18n.t()` 调用

## 阶段 4: 验证与测试
- [ ] 验证登录页面的多语言切换
- [ ] 验证后台主页及各子页面的多语言显示
- [ ] 验证语言偏好在 `localStorage` 中的持久化状态
- [ ] 提交测试报告
